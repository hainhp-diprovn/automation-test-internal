function uscln(a: number, b: number): string {
  if ( a == b ) { return `${a} la UCLN cua ${a} va ${b}`;}
  else if (a > b) {
    if (b == 0 || b ==1) {
      return `${b} la UCLN cua ${a} va ${b}`;
    }else {if ( a % b == 0 ) { return `${b} la UCLN cua ${a} va ${b}`;}
      else 
  }else { if (a < b )}
}

